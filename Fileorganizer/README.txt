Per lo step 2:

Addfile.py è stato provato sulla cmd di windows, inserendo:

"percorsocartella\Addfile.py" --file=nomefile.estensione

Per tutti gli step:
Il programma funziona se si trova nella stessa cartella che contiene la sottocartella 'files'. 
Ad esempio la cartella FileOrganizer deve contenere il programma (Addfile.py o il notebook su jupyter) e la sottocartella 'files'.